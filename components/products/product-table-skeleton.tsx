import { ProductTableSkeleton as GenericProductTableSkeleton } from "@/components/ui/table-skeleton";

// Reexport the component for consistent naming
export const ProductTableSkeleton = GenericProductTableSkeleton;